// Configuration for the Library Management System

// Port number for the HTTP listener (default: 9090)
// Can be overridden via Config.toml or command-line arguments
configurable int serverPort = 9090;
